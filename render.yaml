services:
  - type: web
    name: voice-agent-worker
    env: node
    buildCommand: npm install
    startCommand: npm run worker:start
    plan: free
    envVars:
      - key: NODE_VERSION
        value: 20
