import { TTS } from "@livekit/agents-plugin-elevenlabs"; import * as dotenv from "dotenv"; dotenv.config({ path: ".env.local" }); const t = new TTS({ apiKey: process.env.ELEVENLABS_API_KEY, voiceId: "21m00Tcm4TlvDq8ikWAM" }); async function run() { try { const s = t.synthesize("hello test"); for await (const c of s) { console.log(c); break; } console.log("Success"); } catch (e) { console.error("Error:", e); } } run();
